# Changelog

## Unreleased

- Add opt-in durable environment provisioning with rich multi-variable definitions, read-only planning, reviewed UID adoption, create-only or refresh policy, empty runtime secret slots, state-v3 metadata, and a provider-neutral Action/CLI contract. Legacy branch-owned environment inputs remain unchanged.
- Add an opt-in workspace/spec-only mode that keeps workspace linking and spec state while skipping collections, environments, mocks, monitors, exported assets, generated CI, and generated-CI credential side effects.
